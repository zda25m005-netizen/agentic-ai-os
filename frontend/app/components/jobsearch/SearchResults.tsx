// Deprecated — results rendering now lives in agents/job-search/page.tsx
// (self-contained workspace). Kept as a no-op to avoid a dangling import.
export default function SearchResults() {
  return null;
}
