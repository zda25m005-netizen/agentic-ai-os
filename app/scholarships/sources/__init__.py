"""Scholarship source adapters. Each source implements search(intent) -> Scholarship[].

New sources (official APIs / permitted feeds) can be added as adapters without
touching the search pipeline. No prohibited scraping.
"""
