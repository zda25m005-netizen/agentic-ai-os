"""Mission runtime (v2): long-horizon, resumable objectives.

A Mission is an objective the system pursues across sessions via a graph of
Tasks — created, scheduled, executed, and reviewed until the objective is met.
This package holds the domain model, state machine, and persistence.
"""
