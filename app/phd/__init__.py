"""PhD Finder — doctoral opportunity discovery + eligibility matching.

Reuses the shared StudentProfile and the deterministic eligibility engine from
the scholarships package (a PhD opportunity exposes the same structured entry
requirements). Query + explicit filters are authoritative; the resume/profile
only personalize ranking and never override hard constraints.
"""
