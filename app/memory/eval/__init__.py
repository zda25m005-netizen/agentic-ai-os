"""Local memory evaluation harness (LongMemEval-inspired).

IMPORTANT: the scenarios in `dataset.py` are small, hand-written LOCAL FIXTURES for
regression testing of the memory engine — they are NOT the LongMemEval benchmark and
no external/paper numbers are reproduced or claimed here. Results are computed live
against the current MemoryOrchestrator and emitted as machine-readable dicts.
"""
